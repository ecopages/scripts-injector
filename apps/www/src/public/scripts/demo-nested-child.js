const child = document.getElementById('demo-nested-child');
if (child) {
	child.textContent = 'Child Active!';
	child.className = 'btn btn-primary bg-pink-600 hover:bg-pink-700 border-none shadow-lg';
}
